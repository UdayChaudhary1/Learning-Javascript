"use strict"; //It treats JS code as the newer version of JS

// alert(3+3) //Cannot be used in node.js but only browser
// console.log('asdf') console.log('sdf') it will give error, we can remove error using semi colon but this way is not readable.

// dataTypes
// number, bigint
// string, boolean
// null - It is a standalone value - it means empty
// undefined - value is not assigned
// symbol - unique
// object

console.log(typeof "Hello") // String
console.log(typeof null) // Object
console.log(typeof undefined)// Undefined